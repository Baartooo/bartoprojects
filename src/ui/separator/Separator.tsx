import React from 'react';
import { Box } from '@mantine/core';

import { separatorStyles } from '@/ui/separator/Separator.styles';

export const Separator = () => <Box component={'div'} sx={separatorStyles} />;
