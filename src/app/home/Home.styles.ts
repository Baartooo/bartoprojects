import { Styles } from '@/theme/theme.types';

export const buttonRowStyles: Styles = () => ({
  display: 'flex',
  justifyContent: 'center',
  width: '100%',
});

export const buttonStyles: Styles = () => ({
  marginTop: 32,
});
